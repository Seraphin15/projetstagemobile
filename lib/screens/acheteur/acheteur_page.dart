import 'package:flutter/material.dart';

class AcheteurPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Acheteur'), backgroundColor: Colors.green[800]),
      body: Center(child: Text('Bienvenue acheteur !')),
    );
  }
}
